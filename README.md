# calculadoradecarga

Interactive training-load calculator: live session tonnage (weight × reps × sets) plus per-exercise RIR tracking. Values persist in the browser via localStorage. Support material by Lucas Lima (@personaltrainerlucaslima).
